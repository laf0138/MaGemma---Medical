# Pinned to the versions this codebase is actually tested against - an
# unpinned install months from now (e.g. rebuilding a field kit) can pull
# a materially different, untested version. Bump deliberately and re-run
# the full test suite, don't let these drift silently.
#
# These pins cover DIRECT dependencies only - transitive dependencies
# (Flask's, Socket.IO's, neurokit2's own dependency trees) are still free
# to resolve to a different version on a future install unless you use
# requirements-dev.lock.txt instead, which pins and hashes every package
# in the full resolved tree (regenerate after changing this file:
# `pip install pip-tools && pip-compile --generate-hashes
# --output-file=requirements-dev.lock.txt requirements-dev.txt`, then
# commit the result - `pip install -r requirements-dev.lock.txt` for a
# fully reproducible install).
paho-mqtt==2.1.0
bleak==3.0.2
bleakheart==0.2.0
neurokit2==0.2.13
numpy==2.4.6
requests==2.33.1
flask==3.1.3
flask-socketio==5.6.1
eventlet==0.41.2
pytest==9.1.1
pytest-cov==7.1.0
